#include <stdio.h>

int main() {
	int prime_num, primitive_root;

	printf("Enter a prime number: ");
	scanf("%d", &prime_num);
	printf("Enter a primitive root of the prime number you entered: ");
	scanf("%d", &primitive_root);
	return 0;
}